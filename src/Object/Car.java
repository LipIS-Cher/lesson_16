package Object;

public class Car extends Transport {

    public Car(String brand, String model, String year, String country, String color, String maxSpeed) {
        super(brand, model, year, country, color, maxSpeed);
    }
}
